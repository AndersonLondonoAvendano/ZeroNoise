# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Credentials — never commit
.env

# Templates — sí commitear
# .env.example  ← no ignorar, es plantilla sin credenciales reales

# Proyectos vulnerables de prueba (datos sensibles, archivos grandes)
vuln_projects/

# Logs y outputs generados en runtime
audit.log
artifact_verification_result.json
depcheck_gate_result.json
dependency-check-report.json
*-result.json